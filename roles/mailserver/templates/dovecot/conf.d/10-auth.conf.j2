# /etc/dovecot/conf.d/10-auth.conf — géré par Ansible
disable_plaintext_auth = yes
auth_mechanisms        = plain login

# Authentification système (PAM) désactivée — FreeIPA LDAP uniquement
# !include auth-system.conf.ext

!include auth-ldap.conf.ext
