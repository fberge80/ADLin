# inventory/production/hosts.yml
all:
  vars:
    ansible_user: ansible
    ansible_python_interpreter: /usr/bin/python3

  children:
    ipaservers:
      hosts:
        ipa01.adlin.lab:
          ansible_host: 10.10.10.10

    proxies:
      hosts:
        proxy01.adlin.lab:
          ansible_host: 10.10.10.11

    mailservers:
      hosts:
        mail01.adlin.lab:
          ansible_host: 10.10.10.12

    nextcloud:
      hosts:
        cloud01.adlin.lab:
          ansible_host: 10.10.10.13

    odoo:
      hosts:
        erp01.adlin.lab:
          ansible_host: 10.10.10.14

    chat:
      hosts:
        chat01.adlin.lab:
          ansible_host: 10.10.10.15

    pbx:
      hosts:
        pbx01.adlin.lab:
          ansible_host: 10.10.10.16

    # Toutes les VM Rocky Linux 9 — exclut pbx01 (Debian 12)
    # Cible de 00-common.yml, playbooks/site.yml
    rocky_hosts:
      children:
        ipaservers:
        proxies:
        mailservers:
        nextcloud:
        odoo:
        chat:
